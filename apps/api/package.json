{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -b",
    "dev": "pnpm tsx --watch src/index.ts",
    "start": "pnpm tsx src/index.ts",
    "env:run": "dotenv --",
    "db:generate": "pnpm env:run pnpm --filter=@repo/database db:generate",
    "db:migrate": "pnpm env:run pnpm --filter=@repo/database",
    "db:studio": "pnpm env:run pnpm --filter=@repo/database db:studio",
    "db:push":"pnpm env:run pnpm --filter=@repo/database db:push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google/genai": "^1.39.0",
    "@hono/node-server": "^1.19.9",
    "dotenv": "^17.2.3",
    "hono": "^4.11.7",
    "pdf-to-img": "^5.0.0",
    "tesseract.js": "^7.0.0"
  },
  "devDependencies": {
    "@repo/auth": "workspace:*",
    "@repo/database": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^22.15.3",
    "dotenv-cli": "^11.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
